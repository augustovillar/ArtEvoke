# Database (set in AWS Secrets Manager)
# IMPORTANT: DB_USER and DB_PASSWORD must match db_username and db_password in terraform.tfvars!
DB_USER=admin
DB_PASSWORD=your_password  # Use the same password as in terraform.tfvars
DB_HOST=artevoke-mysql.xxxxx.us-east-1.rds.amazonaws.com
DB_PORT=3306
DB_NAME=artevoke

# Application
STATIC_DIR=/app/data/static
DATA_DIR=/app/data
DISABLE_EMBEDDING_MODEL=false
JWT_SECRET=your_jwt_secret_min_32_chars
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Services
MARITACA_API_KEY=your_maritaca_key

# AWS Credentials (set path to your credentials file)
AWS_SHARED_CREDENTIALS_FILE=/path/to/your/aws/credentials
